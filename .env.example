# Preferred: set an internal backend proxy endpoint that securely calls OpenAI
VITE_AI_PROXY_URL=

# Optional (not recommended for production frontends): direct OpenAI API key
VITE_OPENAI_API_KEY=

# Optional: override the default OpenAI model (default is gpt-4o-mini)
VITE_OPENAI_MODEL=gpt-4o-mini
